# Mobile App (placeholder)

Not started yet — build after the web MVP is validated with the first gym client.

Recommended: React Native (Expo) so you can reuse a lot of logic/patterns
(API service layer, auth context shape) from `frontend/`.

Scaffold later with:
```bash
npx create-expo-app mobile
```
